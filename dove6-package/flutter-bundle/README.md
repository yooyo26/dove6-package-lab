# Flutter Bundle Staging Area

This folder is reserved for the real Dove6 Flutter Linux release bundle.

Expected future source:

build/linux/x64/release/bundle/

Expected bundle contents:

- dove6_client
- lib/
- data/

For now, this folder is only a staging area.
The current package still uses the dummy client in rootfs/opt/dove6/bin/dove6_client.
